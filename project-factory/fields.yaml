activateApis	The list of apis to activate within the project	list(string)	<list>	no
autoCreateNetwork	Create the default network	bool	"false"	no
billingAccount	The ID of the billing account to associate this project with	string	n/a	yes
bucketLocation	The location for a GCS bucket to create (optional)	string	"US"	no
bucketName	A name for a GCS bucket to create (in the bucketProject project), useful for Terraform state (optional)	string	""	no
bucketProject	A project to create a GCS bucket (bucketName) in, useful for Terraform state (optional)	string	""	no
budgetAlertPubsubTopic	The name of the Cloud Pub/Sub topic where budget related messages will be published, in the form of projects/{projectId}/topics/{topicId}	string	"null"	no
budgetAlertSpentPercents	A list of percentages of the budget to alert on when threshold is exceeded	list(number)	<list>	no
budgetAmount	The amount to use for a budget alert	number	"null"	no
credentialsPath	Path to a service account credentials file with rights to run the Project Factory. If this file is absent Terraform will fall back to Application Default Credentials.	string	""	no
defaultServiceAccount	Project default service account setting: can be one of delete, deprivilege, disable, or keep.	string	"disable"	no
disableDependentServices	Whether services that are enabled and which depend on this service should also be disabled when this service is destroyed.	bool	"true"	no
disableServicesOnDestroy	Whether project services will be disabled when the resources are destroyed	string	"true"	no
domain	The domain name (optional).	string	""	no
folderId	The ID of a folder to host this project	string	""	no
groupName	A group to control the project by being assigned groupRole (defaults to project editor)	string	""	no
groupRole	The role to give the controlling group (groupName) over the project (defaults to project editor)	string	"roles/editor"	no
impersonateServiceAccount	An optional service account to impersonate. This cannot be used with credentialsPath. If this service account is not specified and credentialsPath is absent, the module will use Application Default Credentials.	string	""	no
labels	Map of labels for project	map(string)	<map>	no
lien	Add a lien on the project to prevent accidental deletion	bool	"false"	no
name	The name for the project	string	n/a	yes
orgId	The organization ID.	string	n/a	yes
pipExecutablePath	Pip executable path for precondition requirements.txt install.	string	"pip3"	no
projectId	The ID to give the project. If not provided, the name will be used.	string	""	no
pythonInterpreterPath	Python interpreter path for precondition check script.	string	"python3"	no
randomProjectId	Adds a suffix of 4 random characters to the projectId	bool	"false"	no
saRole	A role to give the default Service Account for the project (defaults to none)	string	""	no
sharedVpc	The ID of the host project which hosts the shared VPC	string	""	no
sharedVpcSubnets	List of subnets fully qualified subnet IDs (ie. projects/$projectId/regions/$region/subnetworks/$subnetId)	list(string)	<list>	no
usageBucketName	Name of a GCS bucket to store GCE usage reports in (optional)	string	""	no
usageBucketPrefix	Prefix in the GCS bucket to store GCE usage reports in (optional)	string	""	no
